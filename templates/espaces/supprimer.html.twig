{% extends 'base.html.twig' %}

{% block title %}Supprimer une catégorie{% endblock %}

{% block body %}
    <h1>Supprimer la catégorie <i>{{ espaces.nom }} ?</i></h1>
    {{ form(formulaire) }}

    <a href="{{ path("app_espaces") }}" class="btn btn-default">Appliquer la suppression</a>
{% endblock %}
